/* distinct = removes duplicates, repeated values 
each value appears only once  
syntax 
	SELECT DISTINCT 
*/ 

/* task 1. Return unique list of all countries */
SELECT DISTINCT
	country 
FROM customers 
	